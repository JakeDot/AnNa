# Server Configuration
RUST_LOG=info
PORT=3000

# Set to true (or 1) to enable client mode: the server will accept uploads
# without requiring ?backup=true, acting as both a local client and a sync peer.
# CLIENT_MODE=false

# Database
DATABASE_PATH=./data/metadata.db

# Storage
UPLOADS_DIR=./data/uploads

# GitHub OAuth (required for user login)
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# JWT secret — must be at least 32 random characters
JWT_SECRET=change-me-to-a-long-random-secret

# Public URL of the app (used for OAuth callback redirect)
APP_URL=http://localhost:3000
